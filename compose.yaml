services:
  hm-user-service:
    image: hm-user-service
    build:
      context: .
      dockerfile: Dockerfile
      target: final
      args:
        APP_UID: 1000
        VERSION: ${VERSION:-1.0.0}
    volumes:
      - ./.data:/data